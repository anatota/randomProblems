for(let i = 1; i <= 10; i++) {
    if(isPrime(i)){
        console.log(i);
    }
}

function isPrime(n) {
    if(n < 2) return false;
    let div = 0;
    for(let i = 1; i <= n; i++){
        if(div > 2) return false;
        if(n % i == 0) {
            div++;
        }
    }
    return true;
} 
